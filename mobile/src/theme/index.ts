import { metrics } from './metrics';
import { colors } from './colors';
import { fontSize } from './fonSize';
import { DefaultTheme } from 'styled-components';
export const theme: DefaultTheme = {
  colors,
  fontSize,
  metrics,
};
